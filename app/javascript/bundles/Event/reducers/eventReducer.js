import * as actionTypes from '../constants'

const initialState = {
  name: 'Event Name'
}

export default function (state = initialState, action) {
  switch (action.type) {

    default:
      return state
  }
}
