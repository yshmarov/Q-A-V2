import * as actionTypes from '../constants'

const initialState = {
  email: 'email'
}

export default function (state = initialState, action) {
  switch (action.type) {

    default:
      return state
  }
}
